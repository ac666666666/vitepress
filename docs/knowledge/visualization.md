# 数据可视化

## 1. 常见库对比
- **ECharts**: 百度开源，基于 Canvas，图表类型丰富，配置项多，适合常规报表。
- **D3.js**: 基于 SVG，底层操作 DOM，灵活性极高，学习曲线陡峭。
- **Three.js**: 基于 WebGL，用于 3D 场景渲染。

## 2. Canvas vs SVG
- **Canvas**: 位图，基于像素。适合高性能、数据量大（如 10w+ 点）的场景。不支持事件处理器（需手动计算坐标）。
- **SVG**: 矢量图，基于 DOM。适合缩放不失真、需要交互的场景。DOM 节点过多会影响性能。
